<!--Main Navigation-->
<nav class="navbar navbar-expand-lg navbar-dark bg-light scrolling-navbar">
  <%= link_to root_path, class: "navbar-brand text-muted" do %>
    <%# <%= image_tag('logo.png', class: "img-fluid logo") %>Webstarter
  <% end %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item dropdown">
        <% if admin_signed_in? %>
          <%= link_to 'Dashboard', current_admin %>
          <%= link_to 'Settings', edit_admin_registration_path %>
          <%= link_to "Log Out", destroy_admin_session_path, method: :delete %>
        <% else %>
          <%= link_to "Sign In", new_admin_session_path %>
          <%= link_to "Sign up", new_admin_registration_path %>
        <% end %>
      </li>
    </ul>
  </div>
</nav>
