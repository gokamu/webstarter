

<!-- Navigation -->
  <nav class="navbar navbar-expand-lg navbar-light fixed-top" id="mainNav">
    <div class="container">
     <%= link_to root_path, class: "navbar-brand js-scroll-trigger" do %>
        <%# <%= image_tag('logo.png', class: "img-fluid logo") %>Hekani
      <% end %>
      <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
        Menu
        <i class="fas fa-bars"></i>
      </button>
      <div class="collapse navbar-collapse" id="navbarResponsive">
        <ul class="navbar-nav ml-auto">
          <li class="nav-item">
            <%= link_to 'Registered Schools', schools_path, class: "nav-link js-scroll-trigger" %>
          </li>
          <li class="nav-item">
            <a class="nav-link js-scroll-trigger" href="#features">Features</a>
          </li>
           <li class="nav-item dropdown">
              <% if admin_signed_in? %>
              <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownPortfolio" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Hello <%= current_admin.username %>
              </a>
              <% else %>
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownPortfolio" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Get Started
                </a>
              <% end %>
              <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownPortfolio">
                <% if admin_signed_in? %>
                  <%= link_to 'Dashboard', current_admin, class: "dropdown-item" %>
                  <%= link_to 'Settings', edit_admin_registration_path, class: "dropdown-item" %>
                  <%= link_to "Log Out", destroy_admin_session_path, method: :delete, class: "dropdown-item" %>
                <% else %>
                  <%= link_to "Sign In", new_admin_session_path, class: "dropdown-item" %>
                  <%= link_to "Sign up", new_admin_registration_path, class: "dropdown-item" %>
                <% end %>
              </div>
            </li>
        </ul>
      </div>
    </div>
  </nav>
